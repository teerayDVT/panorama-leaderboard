export class Team {
    constructor(name, played, won, drawn, lost, goalDifference, points) {
        this.name = name;
        this.played = played;
        this.won = won;
        this.drawn = drawn;
        this.lost = lost;
        this.goalDifference = goalDifference;
        this.points = points;
    }
}