import * as fromStandings from './standings.actions';

describe('standingsStandingss', () => {
  it('should return an action', () => {
    expect(fromStandings.standingsStandingss().type).toBe('[Standings] Standings Standingss');
  });
});
